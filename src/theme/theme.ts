import { ChakraTheme } from '@chakra-ui/react';

const theme: Partial<ChakraTheme> = {
    colors: {
        brand: {
            bg: '#FFFCF9',
        },
        black: {
            800: '#2c2c2c',
        },
    },
};

export default theme;
