import { Hind_Siliguri, Inter } from 'next/font/google';

const hindSiliguri = Hind_Siliguri({
    subsets: ['latin'],
    display: 'swap',
    weight: ['400'],
});

export const fonts = {
    hindSiliguri,
};
